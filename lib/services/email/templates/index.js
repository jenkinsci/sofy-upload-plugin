const genericContainer = require('./genericContainer.template');
const register = require('./register.template');
const resetPassword = require('./resetPassword.template');

module.exports = {
    genericContainer,
    register,
    resetPassword,
};
